<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name=".GenTrackApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">

        <!-- Launcher: DashboardActivity — redirects to LoginActivity when unauthenticated -->
        <activity
            android:name=".activities.DashboardActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Auth screen — dark LoginTheme, adjustResize so keyboard pushes card up -->
        <activity
            android:name=".activities.LoginActivity"
            android:exported="false"
            android:theme="@style/LoginTheme"
            android:windowSoftInputMode="adjustResize" />

        <!-- Customer list -->
        <activity
            android:name=".activities.CustomerListActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <!-- Customer detail -->
        <activity
            android:name=".activities.CustomerDetailActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <!-- Bill list for a customer -->
        <activity
            android:name=".activities.BillListActivity"
            android:exported="false" />

        <!-- Bill detail -->
        <activity
            android:name=".activities.BillDetailActivity"
            android:exported="false" />

        <!-- Pricing config -->
        <activity
            android:name=".activities.PricingConfigActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <!-- Announcements — stub for Phase 7 -->
        <activity
            android:name=".activities.AnnouncementsActivity"
            android:exported="false" />

        <!-- PDF sharing -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="com.gentrack.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>
